{
  "buildCommand": "npx expo export --platform web && node scripts/inject-pwa.mjs && node scripts/generate-sw.mjs",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
